diff --git a/include/nesdoug.h b/include/nesdoug.h
index b35dabc..1b5d2f8 100644
--- a/include/nesdoug.h
+++ b/include/nesdoug.h
@@ -164,3 +164,5 @@ void gray_line(void);
 
 void seed_rng(void);
 // get from the frame count. You can use a button (start on title screen) to trigger
+
+void __fastcall__ set_music_volume(unsigned char level);  // 0=silent .. 15=full
diff --git a/include/objects/celestial_bodies.h b/include/objects/celestial_bodies.h
index 065cd44..930107c 100644
--- a/include/objects/celestial_bodies.h
+++ b/include/objects/celestial_bodies.h
@@ -40,4 +40,7 @@ routine(destroy_all_bodies);
 
 extern bool do_collision_checks;
 
+routine(enable_asteroid_spawns);
+routine(disable_asteroid_spawns);
+
 #endif
diff --git a/include/sound.h b/include/sound.h
index 3a60117..98c6930 100644
--- a/include/sound.h
+++ b/include/sound.h
@@ -1,8 +1,9 @@
 #ifndef SOUND_H
 #define SOUND_H
 
-#define BGM_PASSOU 0
+#define BGM_PASSOU  0
 #define BGM_PRELUDE 1
+#define BGM_KNIGHTS 2
 
 #define SFX_EXPLOSION 0
 #define SFX_BLASTER 1
@@ -12,4 +13,9 @@
 
 #define SFX_CHANNEL 0
 
+#define MIN_VOLUME 0
+#define MAX_VOLUME 15
+
+extern bool dim_bgm;
+
 #endif
diff --git a/src/main.c b/src/main.c
index 62f896e..aa2d378 100644
--- a/src/main.c
+++ b/src/main.c
@@ -13,6 +13,7 @@
 #include "objects/pickups.h"
 #include "events.h"
 #include "score.h"
+#include "sound.h"
 
 #pragma bss-name(push, "ZEROPAGE")
 const unsigned char bus_conflict_fix[4]={0,1,2,3};
@@ -24,6 +25,8 @@ val sprid;
 val frame_counter;
 val brightness;
 bool darkening;
+bool dim_bgm;
+val bgm_volume;
 
 static void pal_fade_to(unsigned to)
 {
@@ -43,6 +46,9 @@ main_top: // Jump here if a game over occurs
     brightness = 1;
     frame_counter = 1;
     darkening = true;
+    dim_bgm = false;
+    bgm_volume = MAX_VOLUME;
+    set_music_volume(MAX_VOLUME);
 
     // Turn off the screen
     ppu_off();
@@ -131,6 +137,14 @@ main_top: // Jump here if a game over occurs
         render(CBodies);
         render(Pickups);
         render(Score);
+
+        if (dim_bgm && MIN_VOLUME < bgm_volume) {
+            --bgm_volume;
+            set_music_volume(bgm_volume);
+            if (bgm_volume == MIN_VOLUME) {
+                dim_bgm = false;
+            }
+        }
     }
 
     destroy_all_pickups();
diff --git a/src/nesdoug.S b/src/nesdoug.S
index 19994b3..ff830a7 100644
--- a/src/nesdoug.S
+++ b/src/nesdoug.S
@@ -808,5 +808,13 @@ _seed_rng:
 	sta <RAND_SEED
 	rts
 	
+.export _set_music_volume
+
+; void __fastcall__ set_music_volume(unsigned char level);  // 0..15
+_set_music_volume:
+	and #$0f          ; clamp to 0..15
+	sta volume_Sq1
+	sta volume_Sq2
+	sta volume_Nz
+	rts	
 	
-	
\ No newline at end of file
diff --git a/src/objects/bullets.c b/src/objects/bullets.c
index 3f366fa..7341de0 100644
--- a/src/objects/bullets.c
+++ b/src/objects/bullets.c
@@ -50,6 +50,7 @@ val num_bullets(void) {
 
 static val sprite, attrs;
 void add_bullet(sbigval x, sbigval y, sbigval vx, sbigval vy, facing f) {
+    dim_bgm = true;
     if (n_bullets >= MAX_BULLETS)
         return;
 
diff --git a/src/objects/celestial_bodies.c b/src/objects/celestial_bodies.c
index 39baaf9..fadffa6 100644
--- a/src/objects/celestial_bodies.c
+++ b/src/objects/celestial_bodies.c
@@ -28,6 +28,7 @@ static val n_bodies = 0;
 static CBody bodies[MAX_BODIES];
 static val i, j;
 static val spawn_timer;
+static bool do_spawns = true;
 
 static val out;
 val random_attrs(void) {
@@ -160,7 +161,8 @@ collision_check:
                 spawn_vy = (rand8() >> 1) * (rand8() % 0x1 ? 1 : -1);
             }
             
-            add_body(spawn_x, spawn_y, spawn_vx, spawn_vy, rand8() % CBodyTypeEnd, rand8() < 85, random_attrs());
+            if (do_spawns)
+                add_body(spawn_x, spawn_y, spawn_vx, spawn_vy, rand8() % CBodyTypeEnd, rand8() < 85, random_attrs());
             spawn_timer = 0;
         }
     }
@@ -294,3 +296,11 @@ void add_body(bigval x, bigval y, sbigval vx, sbigval vy, CBodyType type, bool h
 routine(destroy_all_bodies) {
     n_bodies = 0;
 }
+
+routine(enable_asteroid_spawns) {
+    do_spawns = true;
+}
+
+routine(disable_asteroid_spawns) {
+    do_spawns = false;
+}
